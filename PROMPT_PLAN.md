# PROMPT_PLAN.md

---

## Шаг 0: Инициализация окружения
**Промпт:**
Подготовь чистое рабочее окружение для проекта. 
Выполни подготовку проекта строго по следующим шагам:

1. Инициализация Next.js: Создай новый проект Next.js в текущей директории. Используй конфигурацию:
   - App Router: Да
   - TypeScript: Да
   - ESLint: Да
   - Директория src/: Да
   - Import Alias: `@/*`

2. Настройка Tailwind CSS v4: Убедись, что в проекте установлена и корректно подключена именно 4-я версия Tailwind CSS. Проверь конфигурационные файлы (`глобальный css` или файлы настроек сборщика) под стандарты Next.js 16.

3. Установка зависимостей: Установи пакеты, необходимые для реализации бизнес-логики на следующих этапах:
   - Локализация: `next-intl`
   - Работа с формами: `react-hook-form`
   - Валидация: `zod`

4. Инициализация репозитория: 
   - Настрой базовый `.gitignore`, чтобы он игнорировал папку `.next`, `node_modules` и файлы окружения `.env`.
   - Инициализируй git-репозиторий (`git init`).
   - Добавь все созданные файлы в индекс и сделай первый коммит с сообщением "chore: initial commit with next.js, tailwind v4 and core dependencies".

Выведи список установленных версий пакетов из `package.json` и подтверди, что проект готов к запуску через `npm run dev`.

---

## Шаг 1: Структуризация
**Промпт:**
Цель — итеративно реализовать проект LONDON ROUTE TRANSFERS. 
Сейчас делаем только ШАГ 1: структура, стили, глобальный Layout.

Реализуй следующие задачи строго по пунктам:
1. Структура: Создай папки в `src/`: `app/[locale]/`, `components/`, `schemas/`, `mocks/`.
2. Локализация: Настрой `next-intl` для локалей `en` (дефолт) и `ru`. Создай файлы `messages/en.json` и `messages/ru.json` с переводами для Шапки и Подвала.
3. Дизайн-система (Tailwind CSS v4): Настрой палитру Light Premium Sky & Nature. Используй `font-serif` для заголовков и `font-sans` для интерфейса.
4. Макет: Напиши `src/app/[locale]/layout.tsx` (Server Component). Обязательно прими `locale` из `params` (с учетом async), оберни в теги <html> и <body>, импортируй `globals.css`.
5. Компоненты: Сверстай адаптивный Header (с эффектом матового стекла и переключателем языков) и Footer (используй данные: [ВСТАВИТЬ_ДАННЫЕ_РЕКВИЗИТОВ_ИЗ_ТЗ]).
6. Заглушки: Создай пустые роуты для остальных страниц, чтобы проект проходил `npm run build`.

Не пиши форму бронирования или админку. Выведи структуру файлов и код макета.

---

## Шаг 2: Настройка мультиязычности (`next-intl`)
**Промпт:**
Переходим к ШАГУ 2. Реализуй главную страницу `src/app/[locale]/page.tsx` как Server Component.

Требования:
1. Локализация: Полностью переведи русскоязычный контент из ТЗ на английский. Помести английский в `messages/en.json`, русский — в `messages/ru.json`. В коде страницы — никакого захардкоженного текста, только `useTranslations`.
2. Метаданные: Напиши функцию `generateMetadata` для динамического перевода Title и Description.
3. Верстка секций по ТЗ:
   - Hero-секция: [ВСТАВИТЬ ТЕКСТ БЛОКА 1]. Добавь небесный градиент, Serif-заголовок, кнопка ведет на `/book`.
   - О компании: [ВСТАВИТЬ ТЕКСТ БЛОКА 2]. Чистый блок на подложке.
   - Услуги: [ВСТАВИТЬ ТЕКСТ БЛОКА 3]. Список услуг с описанием.
   - Преимущества: [ВСТАВИТЬ ТЕКСТ БЛОКА 4]. Две колонны: "Кому подходят наши услуги" и "Почему выбирают LONDON ROUTE TRANSFERS".

Выведи код страницы и структуру JSON-файлов.

---

## Шаг 3: Статический контент (Лендинг и Legal)
**Промпт:**
Переходим к ШАГУ 3. Задача — внедрить премиальные UI-эффекты и создать интерактивную форму бронирования.

Выполни по пунктам:
1. Эффекты: Настрой `scroll-smooth`, якорные ссылки в Header. Для `public/Background.png` в Hero-блоке настрой: opacity (30-35%), blur, градиентную маску снизу в белый цвет и плавный параллакс-эффект при скролле (translate-y).
2. Журнальный сплит (Секция услуг): Вместо стандартной сетки сделай "Editorial Split Showcase" (grid 12 колонок). Слева (5 колонок) — вертикальный список услуг Serif-шрифтом [ВСТАВИТЬ СПИСОК УСЛУГ]. При наведении пункт становится темным, остальные блекнут, загорается изумрудный маркер. Справа (7 колонок) — демонстрационная зона (Client Component, useState), меняющая контент с анимацией fade-in. На мобильных — аккордеон.
3. Форма бронирования (`src/app/[locale]/book/page.tsx`): Client Component, использующий `react-hook-form` + `zod`. Схему вынеси в `src/schemas/booking.ts`.
4. Поля формы: Реализуй все 12+ полей [ВСТАВИТЬ СПИСОК ПОЛЕЙ ИЗ ФАЙЛА ТЗ]. Оформи в светлом стиле. Тексты ошибок Zod вынеси в JSON-локализацию.
5. Имитация отправки: через `setTimeout` (1.5-2 сек). Показывай лоадер, блокируй кнопку. При успехе — скрывай форму и показывай Success State [ВСТАВИТЬ ДИСКЛЕЙМЕР УСПЕХА].

---

## Шаг 4: Схема валидации и Клиентская форма бронирования
**Промпт:**
Переходим к ШАГУ 4. Нам нужно провести технический аудит роутинга и закрыть долги по валидации.

Задачи:
1. Валидация: В Zod-схему (`src/schemas/booking.ts`) добавь валидацию телефона через regex (международный формат, знаки +, цифры, длина 10-15 символов). Локализуй ошибки.
2. Исправление навигации: В Header замени дефолтные ссылки на локализованный `Link` из конфигурации навигации `next-intl` (чтобы при переходах сохранялся префикс языка `/ru` или `/en`).
3. Синхронизация локалей: Убедись, что структуры `en.json` и `ru.json` идентичны по ключам, и на `/ru` весь контент и ошибки выводятся на русском.

---

## Шаг 5: Моковые данные и Панель менеджера
**Промпт:**
Переходим к ШАГУ 5. Задача — реализовать юридические страницы сайта.

Задачи:
1. Роуты: Создай в `src/app/[locale]/` статические Server Components для путей `/privacy`, `/terms`, `/cookies`. Настрой там `generateMetadata`.
2. Контент: Перенеси юридические тексты [ВСТАВИТЬ ЮРИДИЧЕСКИЙ ТЕКСТ ИЗ ТЗ] в `messages/ru.json` и сделай их качественный перевод на деловой английский для `messages/en.json`.
3. Дизайн: Верстка должна быть чистой, используй теги h1, h2, p, ul, li. Стили Tailwind v4: Serif-заголовки, приглушенный цвет текста (`text-slate-600 leading-relaxed`), минималистичные разделители.
4. Интеграция: В компоненте Footer замени заглушки страниц политик на реальные локализованные компоненты `Link`.

---

## Шаг 6: SEO, Анимации и Финализация
**Промпт:**
Переходим к ШАГУ 6. Последняя задача — хотфикс верстки и создание панели управления заказами.

Задачи:
1. Моковые данные: Создай `src/mocks/bookings.ts`, опиши типы сущности заказа. Сформируй массив из 4-5 реалистичных бронирований, содержащих все 12+ полей формы, уникальный ID (например, #LTR-9402) и статусы (`Pending`, `Confirmed`, `Completed`, `Cancelled`).
2. Панель менеджера (`src/app/[locale]/manager/page.tsx`): Client Component в строгом светлом административном стиле. Выведи заказы в виде интерактивной таблицы или списка карточек.
3. Логика и Стейт: Реализуй смену статуса заказа через выпадающий список (select). Цвет бейджа статуса должен динамически меняться. Изменения сохраняй локально в `useState`.
4. Фильтрация и Локализация: Добавь сверху панель фильтров по статусам заказа. Все тексты панели, названия полей, статусы и Empty State («Ничего не найдено») вынеси в JSON-файлы локализации. Хардкод запрещен.
